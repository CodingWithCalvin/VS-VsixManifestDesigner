<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging">

    <!-- Card Container Style -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource {x:Static vs:VsBrushes.WindowKey}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarMenuBorderKey}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="Margin" Value="0,0,0,16" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.1" Direction="270" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Card Header Container Style -->
    <Style x:Key="CardHeaderStyle" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <!-- Card Header Icon Style -->
    <Style x:Key="CardHeaderIconStyle" TargetType="imaging:CrispImage">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Card Header Title Style -->
    <Style x:Key="CardHeaderTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="8,0,0,0" />
    </Style>

    <!-- Card Content Style - for the content area within a card -->
    <Style x:Key="CardContentStyle" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Vertical" />
    </Style>

    <!-- Card with DataGrid - less padding for grids -->
    <Style x:Key="CardWithGridStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ClipToBounds" Value="True" />
    </Style>

    <!-- Card Inner Header for cards with grids (padding inside) -->
    <Style x:Key="CardInnerHeaderStyle" TargetType="Border">
        <Setter Property="Padding" Value="20,16,20,12" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarMenuBorderKey}}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <!-- Card Footer for button panels -->
    <Style x:Key="CardFooterStyle" TargetType="Border">
        <Setter Property="Padding" Value="20,12" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarMenuBorderKey}}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="Background" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}" />
    </Style>

</ResourceDictionary>
