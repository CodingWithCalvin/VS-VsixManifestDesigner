<UserControl x:Class="CodingWithCalvin.VsixManifestDesigner.Views.AssetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CodingWithCalvin.VsixManifestDesigner.ViewModels"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ManifestViewModel}"
             d:DesignHeight="500" d:DesignWidth="700">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CodingWithCalvin.VsixManifestDesigner;component/Themes/ThemeResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Style="{StaticResource PageHeaderContainerStyle}">
            <StackPanel Style="{StaticResource PageHeaderRowStyle}">
                <imaging:CrispImage Style="{StaticResource PageHeaderIconStyle}"
                                    Moniker="{x:Static catalog:KnownMonikers.FolderClosed}" />
                <TextBlock Style="{StaticResource PageHeaderTitleStyle}" Text="Assets" />
            </StackPanel>
            <TextBlock Style="{StaticResource PageHeaderDescriptionStyle}"
                       Text="Define the files and projects that make up your extension package." />
        </StackPanel>

        <!-- Assets Card -->
        <Border Grid.Row="1" Style="{StaticResource CardWithGridStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Card Header -->
                <Border Grid.Row="0" Style="{StaticResource CardInnerHeaderStyle}">
                    <StackPanel Style="{StaticResource CardHeaderStyle}" Margin="0">
                        <imaging:CrispImage Style="{StaticResource CardHeaderIconStyle}"
                                            Moniker="{x:Static catalog:KnownMonikers.Package}" />
                        <TextBlock Style="{StaticResource CardHeaderTitleStyle}" Text="Assets" />
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          x:Name="AssetsGrid"
                          ItemsSource="{Binding Assets}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="True"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="80" />
                        <DataGridTextColumn Header="Path / Project" Binding="{Binding Path}" Width="200" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Card Footer with Buttons -->
                <Border Grid.Row="2" Style="{StaticResource CardFooterStyle}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="Add" Margin="{StaticResource ButtonSpacing}" Click="AddAsset_Click" />
                        <Button Content="Edit" Margin="{StaticResource ButtonSpacing}" Click="EditAsset_Click" />
                        <Button Content="Remove" Click="RemoveAsset_Click" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
