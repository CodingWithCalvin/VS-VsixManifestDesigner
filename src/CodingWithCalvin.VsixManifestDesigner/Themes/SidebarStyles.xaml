<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging">

    <!-- Sidebar Container Style -->
    <Style x:Key="SidebarContainerStyle" TargetType="Border">
        <Setter Property="Width" Value="220" />
        <Setter Property="Background" Value="{DynamicResource {x:Static vs:VsBrushes.WindowKey}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarMenuBorderKey}}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
    </Style>

    <!-- Sidebar Header Style -->
    <Style x:Key="SidebarHeaderStyle" TargetType="Border">
        <Setter Property="Padding" Value="16,16,16,12" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarMenuBorderKey}}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <!-- Sidebar Header Title Style -->
    <Style x:Key="SidebarHeaderTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,0,0,0" />
    </Style>

    <!-- Sidebar Navigation Button Style -->
    <Style x:Key="SidebarNavButtonStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Left accent bar (visible when selected) -->
                        <Border x:Name="AccentBar"
                                Grid.Column="0"
                                Background="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}"
                                CornerRadius="0,2,2,0"
                                Visibility="Collapsed" />

                        <!-- Content area -->
                        <Border x:Name="ContentBorder"
                                Grid.Column="1"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Hover state -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ContentBorder" Property="Background"
                                    Value="{DynamicResource {x:Static vs:VsBrushes.CommandBarHoverKey}}" />
                        </Trigger>

                        <!-- Selected state -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="AccentBar" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ContentBorder" Property="Background"
                                    Value="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}" />
                        </Trigger>

                        <!-- Focus state -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="ContentBorder" Property="BorderBrush"
                                    Value="{DynamicResource {x:Static vs:VsBrushes.AccentMediumKey}}" />
                            <Setter TargetName="ContentBorder" Property="BorderThickness" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Navigation Icon Style -->
    <Style x:Key="NavIconStyle" TargetType="imaging:CrispImage">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Navigation Label Style -->
    <Style x:Key="NavLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,0,0,0" />
    </Style>

</ResourceDictionary>
