<platformUi:DialogWindow x:Class="CodingWithCalvin.VsixManifestDesigner.Dialogs.AddDependencyDialog"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                         xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                         mc:Ignorable="d"
                         Title="Add Dependency"
                         Width="500" Height="350"
                         WindowStartupLocation="CenterOwner"
                         ResizeMode="NoResize"
                         ShowInTaskbar="False"
                         HasMaximizeButton="False"
                         HasMinimizeButton="False"
                         Background="{DynamicResource {x:Static vs:VsBrushes.ToolWindowBackgroundKey}}">

    <platformUi:DialogWindow.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs:VsBrushes.ToolWindowTextKey}}" />
        </Style>
    </platformUi:DialogWindow.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Source -->
        <Label Grid.Row="0" Grid.Column="0" Content="Source:" VerticalAlignment="Center" />
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="SourceComboBox" Margin="0,0,0,10"
                  SelectionChanged="SourceComboBox_SelectionChanged">
            <ComboBoxItem Content="Manual" Tag="Manual" />
            <ComboBoxItem Content="Project" Tag="Project" />
            <ComboBoxItem Content="Installed" Tag="Installed" />
            <ComboBoxItem Content="File" Tag="File" />
        </ComboBox>

        <!-- Project (shown when Source is Project) -->
        <Label Grid.Row="1" Grid.Column="0" x:Name="ProjectLabel" Content="Project:" VerticalAlignment="Center" Visibility="Collapsed" />
        <Grid Grid.Row="1" Grid.Column="1" x:Name="ProjectGrid" Margin="0,0,0,10" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ProjectTextBox" IsReadOnly="True" />
            <Button Grid.Column="1" Content="..." Width="30" Margin="5,0,0,0" Click="BrowseProject_Click" />
        </Grid>

        <!-- ID -->
        <Label Grid.Row="2" Grid.Column="0" Content="ID:" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="IdTextBox" Margin="0,0,0,10" />

        <!-- Display Name -->
        <Label Grid.Row="3" Grid.Column="0" Content="Display Name:" VerticalAlignment="Center" />
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="DisplayNameTextBox" Margin="0,0,0,10" />

        <!-- Version -->
        <Label Grid.Row="4" Grid.Column="0" Content="Version:" VerticalAlignment="Center" />
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="VersionTextBox" Margin="0,0,0,10" />

        <!-- Location (shown for File source) -->
        <Label Grid.Row="5" Grid.Column="0" x:Name="LocationLabel" Content="Location:" VerticalAlignment="Top" Visibility="Collapsed" />
        <Grid Grid.Row="5" Grid.Column="1" x:Name="LocationGrid" Margin="0,0,0,10" VerticalAlignment="Top" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="LocationTextBox" />
            <Button Grid.Column="1" Content="..." Width="30" Margin="5,0,0,0" Click="BrowseLocation_Click" />
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click" />
            <Button Content="Cancel" Width="75" IsCancel="True" />
        </StackPanel>
    </Grid>
</platformUi:DialogWindow>
